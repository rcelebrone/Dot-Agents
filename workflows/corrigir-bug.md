---
description: Reportar e corrigir um bug ou incidente — aciona o Tech Lead para triagem e RCA
---

Leia `.agents/rules/orchestrator.md` para entender o protocolo da squad.

Atue como o **Tech Lead** (`.agents/rules/techlead.md`).

Execute `skills/quality/triage/SKILL.md` para isolar e entender o problema reportado. Com o diagnóstico em mãos, crie o arquivo de task em `docs/todo/<NNN-nome-kebab>/tasks.md` usando o template `docs/templates/bug.md`, delegue a correção ao **Dev Team** e garanta que o **QA Specialist** valide antes do **Ops** fechar o ciclo.

Siga o fluxo obrigatório definido em `.agents/rules/orchestrator.md`. Não pule etapas.
